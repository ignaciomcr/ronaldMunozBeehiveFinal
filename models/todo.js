//Es la clase todo como tal.
class Todo {
    constructor(idTodo,completado,title,userId) {
        this.idTodo = idTodo;
        this.completado = completado;
        this.title = title;
        this.userId = userId;
    }
} 